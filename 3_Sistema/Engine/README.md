# Engine

Carpeta de distribucion para cliente.

1. Ejecuta `build_engine.ps1` desde la raiz del proyecto.
2. Se compilaran los modulos Cython y se copiara el paquete listo a `Engine/smartgrids_sim`.
3. En notebook/helper, activa el bloque de import desde `Engine/`.
